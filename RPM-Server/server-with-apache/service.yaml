apiVersion: v1
kind: Service
metadata:
  name: rpm-repo-service
  namespace: rpm-repo
spec:
  type: NodePort
  selector:
    app: rpm-repo
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080